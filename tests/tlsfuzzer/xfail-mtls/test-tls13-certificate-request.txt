# Phase T98 / T102 — `test-tls13-certificate-request.py` XFAILs (now empty).
#
# T98 (pre-fix): 3 PASS / 2 XFAIL — `check sigalgs in cert request` and
#       `verify extensions in CertificateRequest` failed because our
#       in-handshake CertificateRequest advertised only the 6-item
#       `config.signature_algorithms` default (RSA-PSS-RSAE +
#       ECDSA-SECP{256,384}R1 + Ed25519) while tlsfuzzer pinned a
#       comprehensive 18-item list (Ed25519 + Ed448 + ECDSA strong→weak
#       + ECDSA-SHA224 + ECDSA-SHA1 + RSA-PSS-RSAE/PSS strong→weak +
#       RSA-PKCS#1 strong→weak).
# T102: rebuilt the in-handshake CR's sigalgs extension with the
#       comprehensive 18-item list verbatim. The list now also includes
#       `rsa_pkcs1_*` and SHA-1 / SHA-224 schemes — these are
#       RFC 8446 §4.2.3-valid for cert-chain signatures, and per-scheme
#       refusal in CV is still enforced by `is_pkcs1_or_legacy_hash`
#       in `handshake/verify.rs`. Both XFAILs flipped to PASS.
#
# All 5 conversations PASS without any active XFAIL.
