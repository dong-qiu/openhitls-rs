# Phase T98 — XFAIL list for tlsfuzzer test-tls13-certificate-request.py
# when run against the openhitls-rs s-server with --verify-client-cert
# (optional mTLS — server requests but does not require a client cert,
# so the script's first sanity step that sends an empty Certificate
# still succeeds).
#
# Status snapshot (T98 baseline): TOTAL 5 / PASS 3 / FAIL 2 / XPASS 0.
#
# Why each remaining FAIL is XFAIL'd today:
#
# * `check sigalgs in cert request`: tlsfuzzer asserts that the
#   server's CertificateRequest only advertises a specific
#   signature_algorithms subset. Our CR sends the full
#   `config.signature_algorithms` list (RSA-PSS-RSAE / ECDSA / Ed25519 /
#   Ed448). Tightening the CR sigalgs to match the server cert key
#   type is RFC 8446 §4.3.2-permitted but not required, and is queued
#   as a follow-up.
#
# * `verify extensions in CertificateRequest`: same root cause — the
#   script enumerates the exact extension set it expects in CR, and
#   our emission set differs (no `certificate_authorities`, no
#   `oid_filters`).
#
# We omit the `:: reason` field deliberately — see run.sh header for
# the long-form rationale (tlsfuzzer's `-X` substring-matches the
# runtime exception text and over-tightens the gate).

check sigalgs in cert request
verify extensions in CertificateRequest
