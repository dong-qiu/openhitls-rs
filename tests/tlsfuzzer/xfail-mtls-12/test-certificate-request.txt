# Phase T108 — `test-certificate-request.py` (TLS 1.2) XFAILs (1 entry).
#
# Post-T108 baseline: 4 PASS / 1 XFAIL / 0 FAIL.
#
# `check sigalgs in cert request`: tlsfuzzer pins the server's CR
# `signature_algorithms` extension to a hardcoded 18-item list. Our
# TLS 1.2 CR sends `config.signature_algorithms` (default 6-item
# subset). Same root cause as the TLS 1.3 mTLS XFAIL closed by T102
# — but the TLS 1.2 CR construction site is separate and would need
# the same comprehensive-list treatment. Deferred (low priority,
# real-world clients don't depend on a specific server CR list).

check sigalgs in cert request
