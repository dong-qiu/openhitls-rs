# Phase T108 — `test-certificate-verify-malformed.py` XFAILs (1 entry).
#
# Pre-T108 baseline: 262/5 FAIL. T108's CV-verify alert-mapping fix
# (`verify_cv12_signature` now maps crypto-layer errors AND
# verify-returned-false to `decrypt_error` per RFC 5246 §7.2.2)
# closed 4 of 5 FAILs.
# Post-T108: 266 PASS / 1 XFAIL / 0 FAIL.
#
# `pad CertificateVerify`: tlsfuzzer sends a CertificateVerify with
# padding after the signature; expects `decode_error` alert; our
# server's response sequence races with tlsfuzzer's runner state
# machine and the test observes the server's CCS arrive when it
# expected the alert. Real TLS state-machine subtlety related to
# the packed-flight read path; queued as a separate phase. The
# server DOES detect and abort on the bad CV — it's a timing /
# ordering mismatch, not silent acceptance.

pad CertificateVerify
