# Phase T108 / T110 — `test-certificate-verify-malformed.py` XFAILs (empty).
#
# T108 (pre-T110): 266/1 — 1 FAIL was `pad CertificateVerify` which
#       padded the CV body past the declared signature length.
# T110: tightened `decode_certificate_verify12` with a strict body
#       length check (RFC 5246 §7.4.8: body is exactly
#       `SignatureAndHashAlgorithm (2) + signature_length (2) +
#       signature`; trailing bytes are decode_error). All 267
#       conversations now PASS without any active XFAIL.
