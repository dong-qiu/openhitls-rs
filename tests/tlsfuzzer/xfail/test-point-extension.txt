# `test-point-extension.py` (TLS 1.2) — 0 XFAIL (fully clean: 9/9 PASS).
#
# The two former `ec_point_formats` strictness XFAILs were closed by the
# tlsfuzzer-strictness pass — `parse_ec_point_formats` now validates the
# extension per RFC 8422 §5.1.2:
#   - `Empty ec_point_formats extension` (zero-length list `<1..>`) → decode_error (50)
#   - `Uncompressed code point missing in ec_point_formats` (uncompressed/0
#     is mandatory) → illegal_parameter (47)
# No active entries.
