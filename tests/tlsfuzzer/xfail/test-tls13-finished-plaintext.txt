# Phase T92 — `test-tls13-finished-plaintext.py` XFAIL (1 entry).
#
# `Unencrypted Finished message`: tlsfuzzer sends the client
# Finished as a *plaintext* handshake record (without applying the
# client-handshake-traffic AEAD). Our server's read loop expects
# this record to be encrypted (because it's after the implicit
# key change at end of ClientHello / EncryptedExtensions); the
# plaintext record decodes as a content-type=22 record carrying
# garbage which we reject — but the alert description we send
# doesn't match what tlsfuzzer expects. Real but minor;
# scheduled for follow-up.

Unencrypted Finished message
