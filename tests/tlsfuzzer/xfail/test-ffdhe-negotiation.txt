# Phase T129 / I105 — TLS 1.2 FFDHE / DHE_RSA negotiation (2 entries).
#
# 39 of 41 conversations PASS. Phase I105 added TLS 1.2 cipher-suite /
# named-group co-negotiation: a (EC)DHE suite is no longer selectable
# when the client's `supported_groups` carries no usable group for
# its key exchange — so `no overlap between groups` now correctly
# yields a fatal `handshake_failure` (the `args/` file sets tlsfuzzer's
# `--alert` to match; see RFC 5246 §7.2.2).
#
# The 2 residual XFAILs are `fallback to non-ffdhe` (the second is the
# `... with secp256r1 advertised` variant). Both offer
# `[TLS_RSA_WITH_AES_128_CBC_SHA, TLS_DHE_RSA_WITH_AES_128_CBC_SHA]`
# with a `supported_groups` list that has no usable FFDHE group, and
# expect the server to fall back to the *static-RSA* suite
# `TLS_RSA_WITH_AES_128_CBC_SHA`.
#
# WON'T-FIX: this would require the `s-server` to offer a static-RSA
# key-exchange suite. Static RSA key exchange has no forward secrecy;
# adding it to satisfy a test would be a deliberate security
# regression in an FS-only server. The co-negotiation logic is
# already correct — there simply is no non-DHE suite in common, so
# `handshake_failure` is the right outcome for our configuration;
# tlsfuzzer just expects a static-RSA-capable server here.

fallback to non-ffdhe
fallback to non-ffdhe with secp256r1 advertised
