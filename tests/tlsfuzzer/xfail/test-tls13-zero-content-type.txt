# Phase T126 — `test-tls13-zero-content-type.py` XFAILs (2 entries).
#
# A "zero content type" record is a TLS 1.3 record whose decrypted
# TLSInnerPlaintext contains no non-zero type octet (RFC 8446 §5.2).
# RFC 8446 §5.1 / §5.4 require the connection to terminate with an
# `unexpected_message` alert.
#
# T126 fixed the alert mapping: `tls_error_to_alert` now maps the
# record-layer "inner plaintext has no content type" error to
# `unexpected_message` (it previously fell through to
# `internal_error`). That closed the 4 handshake-phase conversations
# — 2/8 → 6/8.
#
# 2 conversations remain, both in the *application-data* phase:
#
#   - zero content type during application data
#   - zero content type and padding during application data
#
# Confirmed (tlsfuzzer-strictness pass): the TLS record layer ALREADY
# rejects a zero-content-type record correctly (`parse_inner_plaintext`
# → "inner plaintext has no content type" → unexpected_message — that is
# what T126 used to close the 4 handshake-phase cases). These 2 remaining
# cases fail purely on `s-server` echo-loop send ordering: the test
# observes an echoed `ApplicationData(len=5)` where its state machine
# expects the alert/NST sequence. That is a CLI echo-server / harness
# interaction, NOT a protocol-conformance gap — closing it means
# re-ordering the echo loop's read-vs-write, which risks the other
# app-data scripts for marginal value. Deferred by design.

zero content type during application data
zero content type and padding during application data
