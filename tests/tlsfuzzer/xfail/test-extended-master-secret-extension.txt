# T135 — `test-extended-master-secret-extension.py` (TLS 1.2) XFAIL
# (9 entries). Run with `-d` (args/) to negotiate ECDHE.
#
# 9 PASS / 9 XFAIL. The 9 PASS cover the core RFC 7627 EMS behaviour
# (negotiated / not-offered / present-in-1.2) — regression coverage
# for our EMS three-state policy. The 9 XFAILs are all unsupported
# features or deliberate divergences, none a real EMS bug:
#
# TLS 1.1 (we are TLS 1.2-only — `protocol_version`):
#   - `sanity TLSv1.1`
#   - `extended master secret in TLSv1.1`
#
# TLS 1.2 renegotiation (we do not support renegotiation — the
# mid-session ClientHello is rejected with `unexpected_message`):
#   - `extended master secret with renegotiation`
#   - `renegotiate with EMS in session without EMS`
#   - `renegotiate without EMS in session with EMS`
#
# TLS 1.2 session resumption (the `s-server` does no TLS 1.2 session
# cache / abbreviated handshake, so the resume conversations get a
# full handshake instead of the expected resumed ServerHello):
#   - `EMS with session resume`
#   - `EMS with session resume and SHA384 PRF`
#   - `EMS with session resume without extension`
#
# Strictness (RFC 7627: the EMS extension body MUST be empty; a
# non-empty/malformed body should draw `decode_error` (50). We are
# lenient and accept it — a strictness follow-up, not a security
# issue):
#   - `malformed extended master secret ext`

sanity TLSv1.1
extended master secret in TLSv1.1
extended master secret with renegotiation
renegotiate with EMS in session without EMS
renegotiate without EMS in session with EMS
EMS with session resume
EMS with session resume and SHA384 PRF
EMS with session resume without extension
malformed extended master secret ext
