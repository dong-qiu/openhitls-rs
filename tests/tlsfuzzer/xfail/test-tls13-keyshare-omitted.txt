# Phase T89/T90/T100 — keyshare-omitted XFAILs (now empty).
#
# T89: 3 entries.
# T90: dropped 1 entry (`key_share extension omitted`) after the
#      mapper widened to route `"missing key_share"` substring to
#      `missing_extension` (109).
# T100: dropped the remaining 2 entries (`empty key_share extension`,
#      `empty key_share extension with valid psk_key exhange modes`)
#      after the alert mapper learned `"too short"` / `"invalid length"`
#      → `decode_error` (50). Our `parse_key_share_ch` already returned
#      `"key_share CH: too short"` for these inputs; only the wire alert
#      was wrong. All 5 conversations now PASS without any XFAIL.
