# Phase T92 — `test-tls13-no-unknown-groups.py` XFAILs (4 entries).
#
# tlsfuzzer enumerates every IANA-unknown / experimental group
# codepoint and confirms the server doesn't accidentally pick one.
# We pass 255/259; 2 unique conversations fail (each repeated once
# as sanity-after-test):
#
# - `long list of unknown (between 1 and 259)` and the
#   `... plus a recognised group` variant: ClientHello carries
#   ~258 supported_groups entries (just below the 16-bit length
#   field limit). Our `process_client_hello` walks the list and
#   appears to time out or reject the oversized extension. Likely
#   needs a length-bounded short-circuit in the supported_groups
#   parser. Scheduled for follow-up.
# - `sanity` ×2: shares the same setup as the long-list cases and
#   trips the same path.

long list of unknown (between 1 and 259)
long list of unknown (between 1 and 259) plus a recognised group
sanity
