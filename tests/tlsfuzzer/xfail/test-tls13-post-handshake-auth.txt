# Phase T125 — `test-tls13-post-handshake-auth.py` XFAILs (2 entries).
#
# RFC 8446 §4.6.2 post-handshake client authentication works end to
# end after the I97 transcript fix: `post-handshake authentication`,
# `post-handshake authentication with no client cert` and both
# `sanity` conversations PASS (4/6). The 2 residual failures are
# distinct robustness gaps in `request_client_auth`, NOT transcript
# bugs — each is queued for a follow-up Implementation phase.
#
# - `malformed signature in PHA`: the client sends a CertificateVerify
#   with a bad signature. `request_client_auth` correctly rejects it
#   (signature verification fails) but then returns `Err` and the
#   s-server echo loop drops the connection — the peer sees an abrupt
#   close. tlsfuzzer expects a fatal `decrypt_error` alert (RFC 8446
#   §6.2). Fix: `request_client_auth` must send the mapped fatal alert
#   before returning, extending the T89 alert-before-close discipline
#   to the post-handshake path.
#
# - `post-handshake authentication with KeyUpdate`: the conversation
#   interleaves a KeyUpdate into the post-handshake auth exchange.
#   `request_client_auth`'s read loop expects exactly
#   Certificate / CertificateVerify / Finished and errors out on the
#   unexpected KeyUpdate handshake message. Fix: the post-handshake
#   read loop must process (and key-update on) an interleaved
#   KeyUpdate and continue, the same way the main read path does.
#
# Both are real but separable; XFAIL'd so the 4 PASS gate cleanly.

malformed signature in PHA
post-handshake authentication with KeyUpdate
