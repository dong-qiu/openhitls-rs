# Phase T90 — TLS 1.2 ECDHE-RSA bad-message XFAIL (8 entries).
#
# All 8 conversations (incl. the two `sanity` runs) fail in tlsfuzzer
# with "Server did not send extension(s): ec_point_formats". RFC 4492
# §5.1.2 makes the server-side `ec_point_formats` extension optional;
# RFC 8422 §5.1.2 deprecated it for TLS 1.3 and recommends limiting
# its use in TLS 1.2 to backward-compatibility scenarios. We don't
# echo it. tlsfuzzer's check is OpenSSL-style (which always echoes).
#
# Echoing the extension is a small change to the TLS 1.2 ServerHello
# extension list — but doing it specifically to satisfy this script
# is the wrong reason. Scheduled for T91 only if we hit a real-world
# interop ask.

invalid point (0, 0)
invalid point (0, 1)
invalid point (self-inconsistent)
invalid point (self-inconsistent, y all zero)
padded Client Key Exchange
sanity
truncated ecdh_Yc value
