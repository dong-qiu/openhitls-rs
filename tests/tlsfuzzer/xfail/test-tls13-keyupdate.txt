# Phase T100 / T101 — `test-tls13-keyupdate.py` XFAILs (down to 2).
#
# T100: closed 254 conversations via the alert-mapping + decode_key_update
#       message tweaks (decode_error / illegal_parameter routing). 261/9.
# T101: added cross-record post-handshake handshake-message reassembly
#       (`post_hs_buffer` field on TlsServerConnection +
#       `tls13_server_read_trait_body!` drain-then-refill loop) +
#       tightened `decode_key_update` to require body length == 1.
#       Closed 7 more conversations:
#         - 1/4, 2/3, 3/2, 4/1 fragmented keyupdate msg, appdata between
#           (now correctly rejected with `unexpected_message` per
#           RFC 8446 §5.1)
#         - fragmented keyupdate msg (now reassembled and processed)
#         - large KeyUpdate message (now rejected with `decode_error`
#           per the body-length-must-be-1 rule)
#         - two KeyUpdates, first fragmented, ... (similar reassembly)
#       Now 268/2.
#
# Two FAILs remain — both are tlsfuzzer-vs-RFC behavioural mismatches
# that are NOT TLS protocol bugs:
#
# * `app data split, conversation with KeyUpdate msg`: tlsfuzzer
#   expects the server to defer the echo of partial AppData ("GET")
#   until the full request is received. Our s-server is a literal
#   echo loop — it returns each AppData chunk to the caller as soon
#   as it arrives. The TLS layer is correct (KU is processed,
#   server sends its own KU per RFC 8446 §4.6.3); the test is
#   coupled to a specific application-layer buffering model.
#
# * `two KeyUpdates in one record`: tlsfuzzer expects the server to
#   abort with `unexpected_message` after seeing two KUs in one
#   record (or skip the second silently). RFC 8446 §5.1 explicitly
#   ALLOWS multiple handshake messages per record; §4.6.3 requires
#   the server to respond with its own KU when the second KU has
#   `update_requested = 1`. We do both correctly. The test's
#   expected-alert branch is stricter than the spec.

app data split, conversation with KeyUpdate msg
two KeyUpdates in one record
