# Phase T100 — `test-tls13-keyupdate.py` XFAILs (9 entries).
#
# Pre-T100 baseline: 6/270 PASS. Two T100 fixes closed 254 conversations:
#   1. `decode_key_update` now embeds "decode error" in the
#      "empty body" message → mapped to `decode_error` (50).
#   2. `decode_key_update` "invalid request_update" message now embeds
#      "illegal_parameter" → mapped to `illegal_parameter` (47), which
#      RFC 8446 §4.6.3 mandates for any KeyUpdate request_update value
#      outside {0, 1}.
# Post-T100: **261/270 PASS / 9 FAIL**.
#
# The 9 remaining FAILs are all about cross-record handshake-message
# fragmentation — a real read-loop limitation we noted in the T98
# review (server only consumes ONE handshake message per record, so a
# KeyUpdate split across two records is mishandled). Same class as
# the T91-deferred Finished-padding tests. Closing this needs a
# read-loop refactor that reassembles handshake messages across
# records (RFC 8446 §5.1 explicitly allows this), which is a larger
# T- or R-phase.
#
# Conversation names are the script's literal `conversations[...]`
# keys (deterministic).

1/4 fragmented keyupdate msg, appdata between
2/3 fragmented keyupdate msg, appdata between
3/2 fragmented keyupdate msg, appdata between
4/1 fragmented keyupdate msg, appdata between
app data split, conversation with KeyUpdate msg
fragmented keyupdate msg
large KeyUpdate message
two KeyUpdates in one record
two KeyUpdates, first fragmented, second fragment under new keys together with second KeyUpdate
