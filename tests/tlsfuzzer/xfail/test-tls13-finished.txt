# Phase T91 / T101 — Finished mutation XFAILs (now empty).
#
# T91: closed 66 of 72 conversations by tightening `decode_finished`
#      (verify_data length strict ==) and relaxing `get_body`
#      (header-only msg routes through per-message decoder).
# T101: closed the remaining 6 `pad_right >= 131072` conversations by
#      adding cross-record handshake-message reassembly to the
#      in-handshake server read path (`tls13_server_do_handshake_body!`
#      step 5c + step 6) per RFC 8446 §5.1. tlslite-ng fragments large
#      Finished messages across multiple records; we now buffer
#      `Handshake` content-type plaintext into a local `hs_buffer`
#      and carve out one complete handshake message at a time before
#      handing to `process_client_finished`. The malformed-but-now-
#      reassembled Finished is then rejected via `decode_finished`'s
#      strict length check, giving the spec-required `decode_error`
#      alert.
#
# All 714/714 conversations PASS without any active XFAIL.
