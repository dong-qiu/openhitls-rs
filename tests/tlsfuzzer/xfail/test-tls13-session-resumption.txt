# Phase T119 — `test-tls13-session-resumption.py` XFAILs (3 entries).
#
# T119 wired session resumption into CI by giving the dedicated
# `--psk` / `--ticket-key` s-server instance a deterministic
# `--ticket-key`. The basic TLS 1.3 resumption flow (initial CH → NST
# → re-CH with PSK extension carrying the ticket as identity → server
# decrypts the ticket and resumes) now works end-to-end:
#
#   - sanity ............................. PASS (TLS 1.3 baseline)
#   - session resumption ................. PASS
#   - session resumption - PSK_WITH_DHE ... PASS
#   - sanity ............................. PASS (variant)
#
# 3 XFAILs remain:
#
# * `sanity - TLS 1.2`: the dedicated session-resumption listener is
#   TLS-1.3-only (matches our other curated listeners). tlsfuzzer's
#   `sanity - TLS 1.2` step asks for a TLS 1.2 handshake and the
#   server replies with a protocol-version alert. Closing it needs a
#   server instance that negotiates 1.2 AND 1.3 on the same port
#   (`min_version=1.2 max_version=1.3`), which our CLI doesn't expose
#   today. Tracked separately.
#
# * `use TLS 1.2 ticket in TLS 1.3`: same TLS 1.2 dependency — needs
#   a server that issues a TLS 1.2 RFC 5077 ticket and accepts a TLS
#   1.3 ClientHello presenting that ticket. Cross-version corner case;
#   not a real interop scenario.
#
# * `session resumption - PSK_ONLY`: tlsfuzzer drives the resumption
#   with `psk_ke` mode only (RFC 8446 §4.2.9 mode 0x00 — PSK without
#   DHE). Our server still requires `psk_dhe_ke` (mode 0x01). Closing
#   this is the explicit scope of T120 (psk_ke server-side support:
#   no server key_share in SH, zero-fill the DH input of the key
#   schedule).

sanity - TLS 1.2
use TLS 1.2 ticket in TLS 1.3
session resumption - PSK_ONLY
