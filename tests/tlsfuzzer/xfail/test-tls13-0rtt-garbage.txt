# Phase T94 / T106 / T131 — `test-tls13-0rtt-garbage.py` XFAILs (3 entries).
#
# T94 (pre-T106): 7 XFAILs documenting that all failures involved
#       sending TLS 1.3 early-data (0-RTT) records that our server
#       didn't tolerate gracefully (closes without alert / wrong alert).
#
# T106: added RFC 8446 §4.2.10 "rejected-0-RTT skip" tolerance:
#   * `client_offered_early_data` flag on `ServerHandshake` (set
#     unconditionally when CH carries the `early_data` extension,
#     regardless of whether the PSK validates or
#     `max_early_data_size > 0`).
#   * `tls13_server_do_handshake_body!` Step 1b (HRR retry CH2 read):
#     when CH1 had `early_data`, tolerate up to 16 non-Handshake
#     records between CH1 and CH2, silently dropping each.
#   * Same macro Step 6 (client Finished read): when CH had
#     `early_data` and we did not accept 0-RTT, tolerate up to 16
#     AEAD-decrypt failures (the fake early-data records the client
#     encrypted under keys we don't have, expecting us to "deobfuscate"
#     and skip). The read-buffer is drained manually on each AEAD
#     failure since `open_record` doesn't drain on error.
#
# Closed 4 XFAILs:
#   * `handshake with invalid 0-RTT`
#   * `handshake with invalid 0-RTT and CCS`
#   * `handshake with invalid 0-RTT and CCS between early data records`
#   * `handshake with invalid 0-RTT with fragmented early data`
#
# Post-T106: 7 PASS / 4 XFAIL / 0 FAIL.
#
# T131 (this phase) — duplicate-CCS fix in `tls13_server_do_handshake_body!`
# (HRR path no longer emits a second middlebox-compat CCS after the
# post-HRR ServerHello, RFC 8446 §D.4) auto-closed
# `handshake with invalid 0-RTT and HRR` — tlsfuzzer's state
# machine no longer trips over the spurious second CCS during the
# HRR+0-RTT-garbage sequence. Post-T131: 8 PASS / 3 XFAIL / 0 FAIL.
#
# CLOSED by the Step-6 skip-deadlock fix (2 of the former 3 XFAILs):
#
# * `handshake with 0-RTT, HRR and early data after 2nd Client Hello`:
#   the fake early-data record arrives AFTER an HRR. Post-HRR, 0-RTT is
#   definitively rejected and early data is forbidden (RFC 8446 §4.1.4 /
#   §4.2.10), so the Step-6 rejected-0-RTT skip is now disabled on the HRR
#   path — the undecryptable record draws `bad_record_mac` immediately
#   instead of being skipped (which deadlocked the read).
# * `undecryptable record later in handshake together with early_data`:
#   a fake record is spliced into a PARTIALLY-received client Finished.
#   Step-6 now stops skipping once `hs_buffer` is non-empty (RFC 8446 §5.1
#   — a record interleaved into a partial handshake message is a fatal
#   framing violation) → `bad_record_mac` instead of a deadlock.
# Both accepted alternates of the test (`bad_record_mac` + close).
#
# The 1 remaining XFAIL:
#
# * `handshake with invalid 0-RTT and unknown version (downgrade to
#   TLS 1.2)`: the CH offers supported_versions = [unknown-high, TLS 1.2]
#   (no TLS 1.3) and expects the server to DOWNGRADE and drive a full
#   TLS 1.2 ServerHello flight. Two reasons this stays XFAIL: (a) the CH's
#   only 1.2-usable cipher is static-RSA `TLS_RSA_WITH_AES_128_CBC_SHA`,
#   which our forward-secrecy-only server never offers; (b) genuine in-
#   handshake 1.3→1.2 downgrade across our two per-version connection
#   handlers is a feature we don't implement. The ALERT, however, is now
#   correct: a no-TLS-1.3 ClientHello draws `protocol_version` (70) per
#   RFC 8446 §4.2.1 (it previously mismapped to `handshake_failure` (40)
#   — fixed in I183). Only the downgrade feature remains.

handshake with invalid 0-RTT and unknown version (downgrade to TLS 1.2)
