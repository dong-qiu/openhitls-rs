# Phase T100 / T104 — `test-tls13-signature-algorithms.py` XFAILs (3 entries).
#
# T100: alert-mapper widening closed 253 of 266 FAILs (16/282 → 269/282).
#       Remaining 13 entries were either fragmented-CH-tolerance gaps
#       or empty/boundary fuzz cases.
# T104: CH-side buffer-and-drain (server Step 1) automatically closed
#       all 10 "huge unknown sig-alg list" XFAILs — tlsfuzzer fragments
#       the giant 65K+ byte sigalgs extension across multiple records,
#       and pre-T104 we couldn't reassemble. Post-T104: 279/282 PASS.
#
# 3 remaining FAILs are boundary fuzz cases not addressed by CH
# reassembly:
#
# * `empty list of signature methods`: `signature_algorithms` extension
#   present with body of just the 2-byte zero length. We reject with
#   "too short", tlsfuzzer expects different alert behaviour.
#
# * `fuzz length inside extension to {0,2}`: boundary cases of the
#   inner length field, distinct from the larger fuzz cases that
#   T100's mapper change closed. These reach a different code path
#   and need a per-extension sentinel-length carve-out.

empty list of signature methods
fuzz length inside extension to 0
fuzz length inside extension to 2
