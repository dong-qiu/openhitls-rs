# Phase T90 — TLS 1.2 CCS XFAIL.
#
# tlsfuzzer's TLS 1.2 ChangeCipherSpec test sends a 2-byte CCS during
# the handshake. RFC 5246 §7.1 says CCS payload is a single byte with
# value 1; anything else is technically a protocol error. OpenSSL
# rejects with `unexpected_message`. We currently accept the longer
# payload (read into the record but only check len < 1 for "incomplete"
# elsewhere). Real spec gap, but lower priority than the TLS 1.3 CCS
# rules already locked in by T88. Scheduled for T91.

two bytes long CCS
