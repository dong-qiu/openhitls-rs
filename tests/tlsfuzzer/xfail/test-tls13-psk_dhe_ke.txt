# Phase T119 — `test-tls13-psk_dhe_ke.py` XFAIL (1 entry).
#
# T119 added TLS 1.3 external-PSK support (RFC 8446 §4.2.11): the server
# now accepts an out-of-band PSK configured via the new `--psk` /
# `--psk-identity` CLI flags, in addition to the resumption-PSK path it
# already had (NewSessionTicket → second handshake). Closes 3 of 4
# `psk_dhe_ke` conversations:
#
#   - sanity ........... PASS (x25519 PSK_DHE handshake)
#   - sanity (variant)   PASS
#   - x25519 ........... PASS
#
# The remaining `ffdhe2048` conversation requires the server to negotiate
# the FFDHE-2048 finite-field group (RFC 7919). Our TLS 1.3 server only
# advertises `X25519`, `secp256r1`, `secp384r1`, `secp521r1` in
# `supported_groups`; FFDHE is not in the default list. Adding FFDHE
# support is a separate I- or T-phase (would also unblock other ffdhe-*
# tlsfuzzer scripts). Not a PSK-specific gap; XFAIL'd here so the rest
# of the script (3 PASS) gates cleanly.

ffdhe2048
