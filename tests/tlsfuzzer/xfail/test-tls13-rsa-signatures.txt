# Phase T92 — `test-tls13-rsa-signatures.py` XFAILs (2 entries).
#
# Server fails with `internal_error` (80) when tlsfuzzer offers
# `rsa_pss_rsae_sha384` or `rsa_pss_rsae_sha512` and not the lower
# (sha256) variant. Likely a sign-side gap in our RSA-PSS code
# path when the chosen scheme doesn't pair with our 2048-bit RSA
# cert in the way our handshake code expects, or the scheme isn't
# in our default `signature_algorithms` advertisement. Needs
# investigation; scheduled for follow-up.

tls13 signature rsa_pss_rsae_sha384
tls13 signature rsa_pss_rsae_sha512
